services:
  fks_training:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app/src
    command: ["python", "-m", "main"]
    environment:
      TZ: America/Toronto
      SERVICE_PORT: 4400
      PYTHONPATH: /app/src
    ports:
      - "4400:4400"
    volumes:
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
      - ./logs:/app/logs
      - ./models:/app/models
    networks: [fks_net]
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:4400/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

networks:
  fks_net:
    external: true